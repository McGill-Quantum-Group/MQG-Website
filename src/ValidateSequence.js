import KeyboardListener from "./listeners/keyListener";
import React, { useEffect } from "react";

function ValidateSequence(ref) {
  console.log(KeyboardListener());
}

export default ValidateSequence;
